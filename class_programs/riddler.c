#include <stdio.h>

int main(){

  //create an arr of 100 ints and set 5th element to 10 and 90th to 50 without using loops in one line and rest to 0;
  /* int arr[100] = {[4] = 10, [89] = 50};
  for(int i = 0; i < 100; i++){
    printf("%d", arr[i]);
  } */

  //print a string without using the printf
  //puts("this is crazy!");

  // print without semicolon
  //if (printf("Hello, World!")){}
  return 0;
}